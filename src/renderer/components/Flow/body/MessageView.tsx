import React, { FunctionComponent } from 'react';

type Props = {
  editable?: boolean;
};

const MessageView: FunctionComponent<Props> = ({ editable }) => {
  return <div>MessageView</div>;
};

export default MessageView;
