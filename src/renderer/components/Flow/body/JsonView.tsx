import React, { FunctionComponent } from 'react';

type Props = {
  editable?: boolean;
};

const JsonView: FunctionComponent<Props> = ({ editable }) => {
  return <div>JsonView</div>;
};

export default JsonView;
